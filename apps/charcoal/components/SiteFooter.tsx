/**
 * SiteFooter: intentionally undecided. The footer is a wholly charcoal-black
 * shell so its contents can be designed later without inheriting a premature
 * information architecture.
 */
export function SiteFooter() {
  return <footer className="site-footer" aria-label="Site footer" />;
}
