"use client";

// Re-export: layout imports Providers by path; the implementation (and the
// MotionConfig with reducedMotion="user") lives in components/motion.tsx.
export { Providers } from "@/components/motion";
