# CONTRADICTIONS

Tracks conflicts between specifications, code, architecture, or external dependencies. Every contradiction must be resolved or explicitly cataloged.

---

| ID | Conflict | Sources in Conflict | Resolution Plan | Status |
|---|---|---|---|---|
| **CON-01** | Space nomenclature vs OpenRails nomenclature | `mcosm-OpenRails` uses `pacts`, `paths`, `mandates`. New architecture specifies single noun `Space`. | Internal adapter layer maps Space concepts to OpenRails execution kernel. User/agent APIs expose only `Space`. | `RESOLVED_IN_SPEC` |
| **CON-02** | Settlement target: Arc Testnet vs OKX X Layer | Original Microcosm repo targeted Arc testnet. OKX Dev Day 2026 targets OKX X Layer. | Update Foundry configuration, chain IDs (195/196), RPC endpoints, and USDC addresses for OKX X Layer. | `ACTIVE` |
| **CON-03** | Claim vocabulary cited to the wrong document | `AGENTS.md:10` and `MILESTONES.md` both attribute `UNTESTED · PARTIAL · FAILED · VERIFIED · REGRESSED` to `BUILD_FOUNDRY.md`, which defines no such set; it carries capability states and a gap-closure lifecycle instead. | Claim states are authoritative in `AGENTS.md`; `BUILD_FOUNDRY.md` remains authoritative for capability and blocker states. Citation corrected in `MILESTONES.md`. | `RESOLVED_IN_SPEC` |
| **CON-04** | One blocker, three spellings | `MILESTONES.md` used bare `BLOCKED` for M6; `forge/FAILURES.md` and `docs/canonical/IMPLEMENTATION_STATUS.md` use `BLOCKED_EXTERNAL` for the same EXT-01 blocker. | Standardise on the `BUILD_FOUNDRY.md` capability states. Corrected in `MILESTONES.md`. | `RESOLVED_IN_SPEC` |
| **CON-05** | Duplicate milestone specification | `MILESTONES.md` and `docs/canonical/ROADMAP_AND_MILESTONES.md` were byte-identical (md5 `ca4bf711b82ad0308fd03eab87cb2a1e`) with no declared precedence. | `MILESTONES.md` at the repository root is the single source; the canonical path is now a pointer to it. | `RESOLVED_IN_SPEC` |
| **CON-06** | Demo implied live onchain settlement | The demo printed `X Layer Tx Hash` from a value minted by `crypto.randomBytes`, and `PROOF_LEDGER.md` described the run as passing "live". `AGENTS.md:13-14` forbids claiming a real settlement occurred. | Settlement receipts now carry `simulated: true`; demo output and the ledger label the run a local simulation. | `RESOLVED_IN_SPEC` |
| **CON-07** | Ledger category column claims X Layer for locally executed evidence | Rows CHAIN-1…3, ATTEST-1/2, ADJUD-1/2 and DEPLOY-1 are categorised `X Layer`, but their evidence is contract tests on a local EVM and a `--broadcast` against `127.0.0.1:8545`. No row has X Layer testnet evidence. | Either recategorise those rows as local-EVM contract evidence, or land M6 and re-run them against chain 1952. Needs a call: this is a labelling question, not a code defect, so it is not rewritten unilaterally here. | `ACTIVE` |
| **CON-08** | Testnet chain ID and RPC endpoint are retired | The repo targets "Chain ID 195" on `https://xlayertestrpc.okx.com` (`Makefile`, `.env.example`, deploy script and source comments). That host is dead (IPv6-only DNS, TCP/443 refused) and chain 195 is deprecated. OKX's RPC-endpoints documentation gives the live testnet as **chain 1952** (`0x7A0`) at `https://testrpc.xlayer.tech`; mainnet remains 196 at `https://rpc.xlayer.tech`. | Defaults corrected in `Makefile` and `.env.example`; comments corrected in `DeployXLayer.s.sol`, `AgenticCommerce.sol`, `SettlementRouter.sol`. No code change required — the EIP-712 domain follows `block.chainid` onchain and a `chainId` parameter in the policy engine. Verified by `forge test --fork-url https://testrpc.xlayer.tech` → 36/36 pass. | `RESOLVED_IN_SPEC` |
